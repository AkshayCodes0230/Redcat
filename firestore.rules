rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Visitor counter
    match /stats/{statId} {
      allow read, write: if statId == 'visitors';
    }
    
    // Default deny for everything else
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
