{
  "entities": {
    "VisitorStats": {
      "title": "Visitor Stats",
      "description": "Publicly accessible document to track total site visitors.",
      "type": "object",
      "properties": {
        "count": {
          "type": "integer",
          "description": "Total number of visitors."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last time the counter was updated."
        }
      },
      "required": ["count", "updatedAt"]
    }
  },
  "firestore": {
    "/stats/visitors": {
      "schema": "VisitorStats",
      "description": "Single document holding the global visitor counter."
    }
  }
}
